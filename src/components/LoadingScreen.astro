<div id="screen">
	<div>
		<div class="loader"></div>
	</div>
</div>

<script>
	const screenLoader = document.querySelector("#screen") as HTMLElement

	setTimeout(() => {
		screenLoader.style.display = "none" // Oculta el div
	}, 2000)
</script>

<style>
	#screen {
		z-index: 100;
		position: absolute;
		display: flex;
		justify-content: center;
		align-items: center;
		top: 39px;
		left: 39px;
		width: 1016px;
		height: 568px;
		background: #cbd9ec;
	}

	/* HTML: <div class="loader"></div> */
	.loader {
		position: relative;
		width: fit-content;
		font-weight: bold;
		font-family: sans-serif;
		font-size: 85px;
		padding-bottom: 8px;
		background: linear-gradient(currentColor 0 0) 0 100%/0% 10px no-repeat;
		animation: l2 2s linear infinite;
		color: rgb(60, 70, 88);
	}
	.loader:before {
		content: "Loading...";
		text-shadow:
			4px 4px 0 #cbd9ec,
			/* Sombra abajo-derecha */ -4px 4px 0 #cbd9ec,
			/* Sombra abajo-izquierda */ 4px -4px 0 #cbd9ec,
			/* Sombra arriba-derecha */ -4px -4px 0 #cbd9ec; /* Sombra arriba-izquierda */
	}
	@keyframes l2 {
		to {
			background-size: 100% 10px;
		}
	}
</style>
